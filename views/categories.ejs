<%- include('partials/main-header') -%>

<section class="text-center">
    <h1 class="pb-4">Categories</h1>

    <nav aria-label="breadcrumb">
        <ol class="breadcrumb">
            <li class="breadcrumb-item">
                <a href="/home">Home</a>
            </li>
            <li class="breadcrumb-item active">
                <span>Categories</span>
            </li>
        </ol>
    </nav>
</section>

<section class="pb-4 pt-4">
    <section class="d-flex mb-2 align-items-center">
        <h2>Cookout</h2>
    </section>
    <section class="row row-cols-2 row-cols-lg-5 g-2 g-lg-3">
        <% if(typeof food.cookout !== 'undefined' && food.cookout.length > 0) { %>
            <% food.cookout.forEach(function(recipe, index) { %>
                <a href="/recipe/<%- recipe._id %>" class="col text-center latest_link">
                    <div class="latest_img shadow">
                        <img src="/img/<%- recipe.image %>" alt="<%- recipe.name %>" Loading="lazy">
                    </div>
                    <div class="pt-1"><%- recipe.name %></div>
                </a>
            <% }) %>
            <% } else { %>
                <p>No recipes found.</p>
        <% } %>
    </section>
</section>

<section class="pb-4 pt-4">
    <section class="d-flex mb-2 align-items-center">
        <h2>Baby Shower</h2>
    </section>
    <section class="row row-cols-2 row-cols-lg-5 g-2 g-lg-3">
        <% if(typeof food.babyShower !== 'undefined' && food.babyShower.length > 0) { %>
            <% food.babyShower.forEach(function(recipe, index) { %>
                <a href="/recipe/<%- recipe._id %>" class="col text-center latest_link">
                    <div class="latest_img shadow">
                        <img src="/img/<%- recipe.image %>" alt="<%- recipe.name %>" Loading="lazy">
                    </div>
                    <div class="pt-1"><%- recipe.name %></div>
                </a>
            <% }) %>
            <% } else { %>
                <p>No recipes found.</p>
        <% } %>
    </section>
</section>

<section class="pb-4 pt-4">
    <section class="d-flex mb-2 align-items-center">
        <h2>Quick & Easy</h2>
    </section>
    <section class="row row-cols-2 row-cols-lg-5 g-2 g-lg-3">
        <% if(typeof food.quickEasy !== 'undefined' && food.quickEasy.length > 0) { %>
            <% food.quickEasy.forEach(function(recipe, index) { %>
                <a href="/recipe/<%- recipe._id %>" class="col text-center latest_link">
                    <div class="latest_img shadow">
                        <img src="/img/<%- recipe.image %>" alt="<%- recipe.name %>" Loading="lazy">
                    </div>
                    <div class="pt-1"><%- recipe.name %></div>
                </a>
            <% }) %>
            <% } else { %>
                <p>No recipes found.</p>
        <% } %>
    </section>
</section>

<section class="pb-4 pt-4">
    <section class="d-flex mb-2 align-items-center">
        <h2>Shindig</h2>
    </section>
    <section class="row row-cols-2 row-cols-lg-5 g-2 g-lg-3">
        <% if(typeof food.shindig !== 'undefined' && food.shindig.length > 0) { %>
            <% food.shindig.forEach(function(recipe, index) { %>
                <a href="/recipe/<%- recipe._id %>" class="col text-center latest_link">
                    <div class="latest_img shadow">
                        <img src="/img/<%- recipe.image %>" alt="<%- recipe.name %>" Loading="lazy">
                    </div>
                    <div class="pt-1"><%- recipe.name %></div>
                </a>
            <% }) %>
            <% } else { %>
                <p>No recipes found.</p>
        <% } %>
    </section>
</section>

<section class="pb-4 pt-4">
    <section class="d-flex mb-2 align-items-center">
        <h2>Sundee Dinner</h2>
    </section>
    <section class="row row-cols-2 row-cols-lg-5 g-2 g-lg-3">
        <% if(typeof food.sundee !== 'undefined' && food.sundee.length > 0) { %>
            <% food.sundee.forEach(function(recipe, index) { %>
                <a href="/recipe/<%- recipe._id %>" class="col text-center latest_link">
                    <div class="latest_img shadow">
                        <img src="/img/<%- recipe.image %>" alt="<%- recipe.name %>" Loading="lazy">
                    </div>
                    <div class="pt-1"><%- recipe.name %></div>
                </a>
            <% }) %>
            <% } else { %>
                <p>No recipes found.</p>
        <% } %>
    </section>
</section>

<%- include('partials/footer') -%>



<!--<section class="row row-cols-2 row-cols-lg-5 g-2 g-lg-3 mb-4">
    <% if(typeof categories !== 'undefined' && categories.length > 0) { %>
        <% categories.forEach(function(category, index) { %>
            <a href="/categories/<%- category.name %>" class="col text-center latest_link">
                <div class="latest_img shadow">
                    <img src="/img/<%- category.image %>" alt="<%- category.name %>" Loading="lazy">
                </div>
                <div class="pt-1"><%- category.name %></div>
            </a>
        <% }) %>
    <% } %>
</section>

<section class="row row-cols-2 row-cols-lg-5 g-2 g-lg-3">
    <% if(typeof categoryById !== 'undefined' && categoryById.length > 0) { %>
        <% categoryById.forEach(function(recipe, index) { %>
            <a href="/recipe/<%- recipe.id %>" class="col text-center latest_link">
                <div class="latest_img shadow">
                    <img src="/img/<%- recipe.image %>" alt="<%- recipe.name %>" Loading="lazy">
                </div>
                <div class="pt-1"><%- recipe.name %></div>
            </a>
        <% }) %>
        <% } else { %>
            <p>No recipes found.</p>
    <% } %>
</section>-->